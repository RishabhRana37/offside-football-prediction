services:
  - type: web
    name: offside-football-prediction
    runtime: python
    buildCommand: "bash build.sh"
    startCommand: "streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
